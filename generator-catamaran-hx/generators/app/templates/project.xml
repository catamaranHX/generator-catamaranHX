<?xml version="1.0" encoding="utf-8"?>
<project>
	<meta title="<%= title %>" package="com.<%= title %>" version="1.0.0" company="<%= company %>" />
	<app main="<%= main_class %>" path="Export" file="CatamaranHx"  preloader="Preloader" />
	<source path="src" />
   	<haxelib name="CatamaranHx" />
	<haxelib name="svg" />
    <haxelib name="openfl" />
    <haxelib name="actuate" />
	<assets path="assets" rename="assets" />
	<!-- THIS IS NEEDED BY THE PRELOADER -->
	<!-- Only HTML5 and FLASH use the preloader, not native -->
	<assets path="assets/preloader" include="*" if="web" />
</project>